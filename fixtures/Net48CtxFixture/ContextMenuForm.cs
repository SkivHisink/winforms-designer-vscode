namespace SampleApp
{
    // The companion partial the sample .Designer.cs omits (it is a render fixture, not a compilable form): declares
    // the Form base and a public parameterless ctor so the net48 engine can instantiate SampleApp.ContextMenuForm.
    partial class ContextMenuForm : System.Windows.Forms.Form
    {
        public ContextMenuForm()
        {
            InitializeComponent();
        }
    }
}
