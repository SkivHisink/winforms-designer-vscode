using System.Windows.Forms;

namespace SampleApp
{
    // The form's code-behind partner. Open THIS file to get the designer (Visual Studio style:
    // SampleForm.cs → designer; SampleForm.Designer.cs is the generated layout). The engine renders
    // the sibling .Designer.cs.
    public partial class SampleForm : Form
    {
        public SampleForm()
        {
            InitializeComponent();
        }
    }
}
