using System.Windows.Forms;

namespace SampleApp
{
    // Code-behind partner for the resx image fixture (excluded from compilation; the engine parses the
    // .Designer.cs and interprets it against a live Form).
    public partial class ImageForm : Form
    {
        public ImageForm()
        {
            InitializeComponent();
        }
    }
}
