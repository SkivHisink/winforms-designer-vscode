using System.Windows.Forms;

namespace SampleApp
{
    // The form's code-behind partner. Open THIS file to get the designer; the engine renders the sibling
    // CustomForm.Designer.cs, which references CustomControls.GaugeControl (resolved via the project's
    // built assembly or an explicit assemblyPath).
    public partial class CustomForm : Form
    {
        public CustomForm()
        {
            InitializeComponent();
        }
    }
}
